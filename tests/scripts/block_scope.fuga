(Block scope test: Variables declared in a block should not be visible outside)
func i32 #main = {
    try {
        var i32 $a = 1.
    } catch 1 {
    }.
    
    (Error: $a was declared in the try block, so it cannot be referenced here after exiting the block)
    var i32 $b = $a.
}.
